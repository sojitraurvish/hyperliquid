export const BASE_URL = "/api/v1";
export const API_ROUTES = {
    GUEST_AUTH: BASE_URL + "/auth/guestAuth", // Changed from guest-auth to guestAuth
    REFRESH_TOKEN: BASE_URL + "/auth/refreshToken", // Add refresh token route
}